#pragma once

// Stub — full implementation in Phase 2 T4.

namespace lumen::plot {

/// Axis orientation.
enum class AxisOrientation { Horizontal, Vertical };

/// Axis for a plot — owns tick generation and label.
/// Full implementation deferred to T4.
class Axis {
public:
    Axis() = default;
};

} // namespace lumen::plot
