#pragma once

// Stub — full implementation in Phase 2 T4.

namespace lumen::plot {

/// Top-level plot container — owns axes, series, title, legend.
/// Full implementation deferred to T4.
class PlotScene {
public:
    PlotScene() = default;
};

} // namespace lumen::plot
