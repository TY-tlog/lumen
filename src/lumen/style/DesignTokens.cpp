#include "DesignTokens.h"

// DesignTokens is header-only (constexpr/inline values).
// This .cpp exists to satisfy the build system and allow future
// non-constexpr additions if needed.

namespace lumen::tokens {
// intentionally empty
}  // namespace lumen::tokens
